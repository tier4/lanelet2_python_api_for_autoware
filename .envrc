# Poetry virtual environment setup with library paths
VENV_PATH=$(poetry env info --path 2>/dev/null || echo "")
if [[ -n "$VENV_PATH" ]]; then
    export LD_LIBRARY_PATH="$VENV_PATH/lib/python3.10/site-packages:$LD_LIBRARY_PATH"
fi